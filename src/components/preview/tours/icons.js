import React from 'react'
import { graphql, useStaticQuery } from 'gatsby'

const PreviewToursIcons = ({ node }) => {
	return (
		<div>
			{/*<img src={node.season.image.publicURL} alt="" title={node.season.name} />*/}
			{/*<img src={node.priceType.image.publicURL} alt="" title={node.priceType.name} />*/}
			{/*<img*/}
			{/*	src={data.strapiCategories.icon.image.publicURL}*/}
			{/*	alt=""*/}
			{/*	title={data.strapiCategories.icon.name}*/}
			{/*/>*/}
		</div>
	)
}

export default PreviewToursIcons
